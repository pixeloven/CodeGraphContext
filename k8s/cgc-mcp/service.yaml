apiVersion: v1
kind: Service
metadata:
  name: cgc-mcp
  labels:
    app: cgc-mcp
    app.kubernetes.io/part-of: codegraphcontext
spec:
  type: ClusterIP
  selector:
    app: cgc-mcp
  ports:
    - name: http-mcp
      port: 8045
      targetPort: http-mcp
      protocol: TCP
