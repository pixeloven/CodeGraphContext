apiVersion: v1
kind: Service
metadata:
  name: cgc-memory
  labels:
    app: cgc-memory
    app.kubernetes.io/part-of: codegraphcontext
spec:
  type: ClusterIP
  selector:
    app: cgc-memory
  ports:
    - name: mcp
      port: 8766
      targetPort: mcp
      protocol: TCP
